#include <stdio.h>
#include "main.h"

/**
 * main - Write a function that takes a pointer to an int
 *
 * description - it updates the parameter int to the value it points to
 *Return: always 0
 */

int main(void)
{
	_putchar(char *);

	int = n;
	int = *n;
	for n = 98
	_putchar ("%d\n, n");
	_putchar ("\n");
	return (0);
}
