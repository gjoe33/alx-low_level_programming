#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point
 * descrition - print numbers od two two digits
 */

void main(void)
{
	putchar("print numbers of two two digits ftom 0-99");
	putchar("the numbers should be separed by , and a space");
	putchar("numbers should be printed in two, 1 should be printed as 01");
	purchar("the two numbers should be separed by a space");
	putchar("the numbers should be printed in acending order");
	putchar("00 01,0 100 are considered same combination of the numbers 0,1");
	return (0);
}
