#include <stdlib.h>
include < time.h >

/*
 *program will assign random number to the variable n each time it is executed
 */

/*
 * main- this is the main function
 * description-to print the last digit of the number stored in the variable n
 *
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2

/*
 * if Last digit of n > 5 the string and is > 5
 * if Last digit of n < 6 and not 0 the sting and is < 6 not 0
 * if Last digit of n = 0 the string and is 0
 *
 */
		print("%d/n", n)

		return(n)

}

