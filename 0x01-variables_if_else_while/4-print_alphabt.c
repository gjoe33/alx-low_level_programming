#include <stdlib.h>
#include <time.h>
/*
 * program to write alphabets in lowcase except q and e
 */

/**
 * main - to write alphabets in lowcase
 * description - to write all alphabet exept q and e
 */
void main(void)
{
	strand(time(NULL));
	putchar(int "write all alphabet in lowcase except q and e")
		putcat(int "write alphabet a to z except q and e in lowcase")
}

