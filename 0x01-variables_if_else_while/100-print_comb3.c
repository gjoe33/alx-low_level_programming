#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry poinr
 * description - print all combination of two digit
 */

void main(void)
{
	putchar("print a  combination of all two digit numbers.\n");
	putchar("separet the numbers with ,");
	putchar("the two digits must be diffrent");
	putchar("print the numbers in assending order");
	putchar("print only the smalllest combination of two numbers");
	return (0);
}
