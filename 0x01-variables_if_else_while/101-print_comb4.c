#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point
 * description - print all the combination of three digits
 */

void main(void)
{
	putchar("prints all possible different combinations of three digits.\n");
	putchar("Numbers must be separated by ,, followed by a space");
	putchar("The three digits must be different");
	putchar("012,210 are considered same combination the 3 digits 0, 1 and 2");
		putchar("Print only the smallest combination of three digits");
		putchar("Numbers should be printed in accending order");
		return (0);
}
